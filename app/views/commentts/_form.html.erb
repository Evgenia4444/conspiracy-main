<%= form_for([@post, @post.commentts.build]) do |f| %>
	
	<div class="field">
        <%= current_user.email %>
    </div>
	<div class="field">
		<%= f.label :body %><br />
		<%= f.text_area :body %>
	</div>
		<div class="actions">
		<%= f.submit %>
		</div>
	</div>
<% end %>